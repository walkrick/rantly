<%= button_to "Logout", root_path, :id => "reg-btn",
              :class => "inbox-sf-add-btn tip", :method => :get %>
<%= notice %>



<%#= link_to "name", edit_users_path %>

<h2>Ranters</h2>
<table class="table">
  <thead>
  <tr>
    <th>Name</th>
  </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to (user).username, user_path(user) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
